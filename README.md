# agency-lang README

Clone this into `~/.vscode/extensions`.

Alternatively, clone this somewhere, then inside VS Code, trigger the command palette using command-shift-P and choose Install Extension from Location. Then select the agency-vscode-extension folder that you just cloned.

Remember to run `make` to build the extension before using!